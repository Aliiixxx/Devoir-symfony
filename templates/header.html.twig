<header>
    <div class="container">
        <div class="header-content">
            <div class="logo">
                <img src="{{ asset('pictures/logo.png') }}" alt="Logo" height="50">
                <p><strong>Don't compromise on your look</strong></p>
            </div>

            <nav>
                <ul>
                    <li><a href="{{ path('app_home') }}">Accueil</a></li>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a href="{{ path('product_list') }}">Boutique</a></li>
                        <li><a href="{{ path('cart_index') }}">Panier</a></li>
                        <li><a href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    {% else %}
                        <li><a href="{{ path('app_register') }}">S'inscrire</a></li>
                        <li><a href="{{ path('app_login') }}">Se connecter</a></li>
                    {% endif %}
                </ul>
            </nav>
        </div>
    </div>
</header>

<style>
.container {
    max-width: 1200px;
    padding: 0 15px;
    
}

.header-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 0;
}

.logo {
    display: flex;
    flex-direction: column;
    justify-content: center;
}

.logo img {
    margin-right: 10px;
}

.logo p {
    margin: 0;
    font-size: 12px;
    color: #666;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
    margin: 0;
    padding: 0;
}

nav ul li {
    display: inline;
}

nav ul li a {
    text-decoration: none;
    color: #000;
    font-weight: normal;
}

nav ul li a:hover,
nav ul li a:active,
nav ul li a:focus {
    font-weight: bold;
}

nav ul li:first-child a {
    font-weight: bold;
}
</style>
